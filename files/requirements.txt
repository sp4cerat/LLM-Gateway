# LLM Gateway v1.5 - Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# HTTP Client
httpx>=0.27.0

# Data Validation
pydantic>=2.5.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Retry Logic
tenacity>=8.2.0

# Numerical (for embeddings/cosine similarity)
numpy>=1.26.0

# Free Data APIs (real-time data without web search costs)
yfinance>=0.2.36          # Stocks: 100% free, no API key
ddgs>=6.0                     # Web/news search: 100% free, no API key (replaces duckduckgo_search)
textblob>=0.18.0          # Sentiment analysis: 100% free, local
# Note: Open-Meteo needs only httpx (already included)
# Note: NewsAPI.org needs only httpx + NEWSAPI_KEY in .env
# Note: Tavily (optional) needs only httpx + TAVILY_API_KEY in .env

# Template/Static files
jinja2>=3.1.0
aiofiles>=23.2.0

# Vision Pipeline (image preprocessing + OCR)
Pillow>=10.0.0            # Image resizing, grayscale, format conversion
pytesseract>=0.3.10       # Fallback OCR (if PaddleOCR unavailable)
# Note: Also install system package: sudo apt install tesseract-ocr tesseract-ocr-deu

# PaddleOCR (primary OCR engine, much better accuracy than Tesseract)
paddlepaddle>=2.6.0       # PaddlePaddle framework (CPU)
paddleocr>=2.8.0          # PaddleOCR with layout detection
# Note: First run downloads ~300MB of models. Uses ~500MB-1GB RAM.
# Install: pip install paddlepaddle paddleocr --break-system-packages

# Document Extraction (for file upload support)
python-docx>=1.1.0        # DOCX text extraction
openpyxl>=3.1.0           # XLSX spreadsheet reading
pymupdf>=1.24.0           # PDF text extraction (fast, best quality)
# Fallbacks: pdfplumber or pypdf (install if pymupdf fails on your system)

# Web Enrichment & Fact-Checking (deep search with full-text extraction)
trafilatura>=1.12.0       # Web page text extraction (best quality, used by web_enrichment.py)
# Note: Falls back to basic HTML stripping if trafilatura unavailable
# Install: pip install trafilatura --break-system-packages

# Speech-to-Text (optional — Browser Speech API is always available as free tier)
# faster-whisper>=1.0.0   # Local Whisper: pip install faster-whisper --break-system-packages
# Note: Groq Whisper API (free tier) is used first if GROQ_API_KEY is set
# Note: faster-whisper downloads ~140MB model on first use (base), runs on CPU
